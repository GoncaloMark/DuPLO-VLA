pip3 install torch torchvision torchaudio --index-url https://download.pytorch.org/whl/cu121 (change cu121 to cuda version in use)

cd policy && pip install -e . && cd ..

pip install zarr wandb ipdb gpustat dm_control omegaconf hydra-core dill einops diffusers numba moviepy imageio av matplotlib termcolor transformers